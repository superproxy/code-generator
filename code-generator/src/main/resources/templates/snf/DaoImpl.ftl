/**
* SUNING APPLIANCE CHAINS.
* Copyright (c) 2012-2012 All Rights Reserved.
*/
package ${package};
import com.suning.dal.dao.impl.BaseDaoImpl;
import com.suning.dal.model.${moduleName}.${shortClassName};

/**
* @author ${author}
* @version 1.0.0
*/
public class ${className} extends BaseDaoImpl {

private final String  ${"create_${name}"?upper_case}="${name}.create_${name}";
private final String  ${"update_${name}"?upper_case}="${name}.update_${name}";
private final String  ${"query_${name}_by_id"?upper_case}="${name}.query_${name}_by_id";


}

